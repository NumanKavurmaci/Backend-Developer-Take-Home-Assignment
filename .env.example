# Local development credentials matching compose.yaml.
# Copy this file to .env for local development.
# Never commit real deployment credentials.

DATABASE_URL="postgresql://saatcms:saatcms_local@localhost:5432/saatcms?schema=public"
NODE_ENV="development"
DEPLOYMENT_ENV="local"

# Optional deployed-demo variables.
# Replace these only inside your private .env file.

# DATABASE_URL="postgresql://USER:PASSWORD@HOST:5432/DATABASE?schema=public"
# NODE_ENV="production"
# DEPLOYMENT_ENV="demo"
# DEMO_DATABASE_CONFIRMATION="HOST/DATABASE/public"
# DEPLOYMENT_URL="https://your-service.onrender.com"

# Local-only catalog generation guards. The 940 MB database guard reserves
# 60 MB of the 1 GB allowance. Select the final row target after measurement.
# CATALOG_MAX_SHOWS=650
# CATALOG_MAX_EPISODES_PER_SHOW=2000
# CATALOG_MAX_CONTENT_ROWS=50000
# CATALOG_MAX_NORMALIZED_ARTIFACT_BYTES=99614720
# CATALOG_MAX_ESTIMATED_DATABASE_BYTES=940000000

# Safe local catalog-build rehearsal defaults are 5 Shows, 50 Episodes per
# Show, 500 Content rows, and 1 TVmaze index page. Override explicitly only
# after inspecting and validating the rehearsal artifact.
# CATALOG_TVMAZE_START_PAGE=0
# CATALOG_TVMAZE_MAX_PAGES=1
# CATALOG_CACHE_DIR=.cache/catalog
# CATALOG_OUTPUT_DIR=data/catalog/current
# CATALOG_OFFLINE=false
