# Local test credentials matching compose.yaml.
# Tests must never point to a deployed or production database.

DATABASE_URL="postgresql://saatcms:saatcms_local@localhost:5432/saatcms_test?schema=public"
NODE_ENV="test"
DEPLOYMENT_ENV="test"