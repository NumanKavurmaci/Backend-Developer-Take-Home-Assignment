# Archived SQLite Migration History

This directory preserves the migration history used before the PostgreSQL
provider switch. It is historical reference only and must not be passed to
Prisma Migrate or applied to an active environment.

The active PostgreSQL migration history lives in `prisma/migrations/`.

